props passing, like string,number,boolean.object and array

ADVANCED PROPS: 
    union props(success,loading,error) except three if any other text enter tsx shows error
    wrap text inside component <demo>im children</demo>
    wrap component inside component <demo> <greet> im wrap component</greet> </demo>
    optional prop if the props is no need to this component means in [type OptProps ={status ?:string}] "put question mark"

HANDLE EVENTS:
    we need to give type to 
                    CLICK event:- event:React.MouseEvent<HTMLButtonElement>    
                    CHANGE event:- event:React.ChangeEvent<HTMLButtonElement>
    if you pass style as props it also you need to give type React.CSSProperties
